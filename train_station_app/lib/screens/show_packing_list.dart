import 'package:flutter/material.dart';

class ShowPackingList extends StatelessWidget {
  const ShowPackingList({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: Text('CHECKLIST'),
      ),
    );
  }
}
